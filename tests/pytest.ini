#[pytest configuration]
[pytest]
markers =
    integration: marks tests as integration (may call real external services)

# NOTE:
# 该文件已移动到 tests/ 目录。若你在项目根目录直接执行 `pytest`，
# Pytest 默认不会自动向下搜索此配置文件。使用以下任一方式确保配置生效：
# 1) 在 tests 目录中运行:  cd tests && pytest
# 2) 在项目根目录运行并显式指定:  pytest -c tests/pytest.ini
# 3) 在根目录创建一个最小的 pytest.ini 再 include（可选，不需要则忽略）
